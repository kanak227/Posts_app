<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-8">

        <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block">Log Out</a>
        

        <h5>Edit your post</h5>

        <form action="/update/<%= post._id %>" method="post" class="text-white">
            <textarea class="w-1/3 resize-none bg-transparent border-2 border-zinc-200 rounded-md mt-5 p-3 outline-none block" name="content"><%= post.content %></textarea>
            <input type="submit" class="px-2 py-2 bg-yellow-500 text-black block rounded-md mt-2 w-40" value="save post">
        </form>

        

    </div>
</body>
</html>